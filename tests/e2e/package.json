{
  "name": "@projectx/e2e",
  "version": "0.1.0",
  "private": true,
  "description": "G5 — end-to-end proof that a demo account can trade: live chart, order, fill, position, P&L, ledger.",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}

{
  "name": "@projectx/feed-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "13-lp-connectivity — provider feeds behind one adapter interface. Owns no financial truth.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.js",
    "typecheck": "tsc --noEmit",
    "test": "node --test \"src/**/*.test.js\""
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.9.3"
  }
}
